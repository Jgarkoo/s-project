import { ConvertImgPipe } from './convert-img.pipe';

describe('ConvertImgPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertImgPipe();
    expect(pipe).toBeTruthy();
  });
});
